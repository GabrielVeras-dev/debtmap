# Copie este arquivo para .env e preencha os valores reais

SPRING_PROFILES_ACTIVE=dev

DATABASE_URL=jdbc:postgresql://localhost:5432/debtmap_db
DB_USERNAME=debtmap_user
DB_PASSWORD=debtmap_pass

# Gere um secret seguro com: openssl rand -base64 64
JWT_SECRET=troque-isso-por-um-secret-real-de-256-bits-minimo